import { AppShell } from "@/components/shell/AppShell";
import { albums, artists, playlists, songs } from "@/lib/music/catalog";
import { Card } from "@/components/ui/Card";

export default function AdminDashboardPage() {
  return <AppShell><div className="mx-auto max-w-7xl py-14"><p className="text-pulse">Administrator</p><h1 className="text-5xl font-black">Admin Dashboard</h1><div className="mt-8 grid gap-4 md:grid-cols-5"><Card><strong>Users</strong><p className="mt-2 text-3xl font-black">12.8K</p></Card><Card><strong>Artists</strong><p className="mt-2 text-3xl font-black">{artists.length}</p></Card><Card><strong>Songs</strong><p className="mt-2 text-3xl font-black">{songs.length}</p></Card><Card><strong>Albums</strong><p className="mt-2 text-3xl font-black">{albums.length}</p></Card><Card><strong>Playlists</strong><p className="mt-2 text-3xl font-black">{playlists.length}</p></Card></div><section className="mt-10 grid gap-6 lg:grid-cols-3"><Card><h2 className="text-2xl font-black">User Management</h2><p className="mt-3 text-slate-400">Review account health, accessibility settings, and library integrity.</p></Card><Card><h2 className="text-2xl font-black">Artist Management</h2>{artists.map((artist) => <p key={artist.id} className="mt-3 rounded-2xl bg-white/10 p-3">{artist.name}</p>)}</Card><Card><h2 className="text-2xl font-black">Music Management</h2>{songs.slice(0, 4).map((song) => <p key={song.id} className="mt-3 rounded-2xl bg-white/10 p-3">{song.title}</p>)}</Card><Card><h2 className="text-2xl font-black">Reports</h2><p className="mt-3 rounded-2xl bg-emerald-400/10 p-3 text-emerald-200">All reports reviewed</p></Card><Card><h2 className="text-2xl font-black">Analytics</h2><p className="mt-3 text-slate-400">Catalog plays, saves, completion rates, and moderation outcomes.</p></Card><Card><h2 className="text-2xl font-black">Content Moderation</h2><p className="mt-3 text-slate-400">Review metadata quality, artwork safety, and audio availability.</p></Card></section></div></AppShell>;
}
